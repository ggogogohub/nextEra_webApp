import React from 'react';
import { WordmarkContainer, LogoText } from './styles';

const LogoWordmark: React.FC = () => {
  return (
    <WordmarkContainer>
      <LogoText>NextEra</LogoText>
    </WordmarkContainer>
  );
};

export default LogoWordmark; 